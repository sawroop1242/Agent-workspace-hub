# Junie FreeLLMAPI Test

This file exists only to create a reviewable test diff for Junie's custom OpenAI-compatible model profile.

The profile uses the `auto` model identifier, `OpenAICompletion` API type, and reads the credential from the `FREELLMAPI_API_KEY` environment variable. No credential value is stored in the repository.
