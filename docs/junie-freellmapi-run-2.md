# Junie FreeLLMAPI Run 2

Test-only change to trigger the Junie review workflow using the FreeLLMAPI OpenAI-compatible custom model profile.

No credentials are stored in this file or repository.

## Run 2 retry marker

This update intentionally changes the test file so the open PR receives a new commit and Junie can be re-tested against the current workflow configuration.
